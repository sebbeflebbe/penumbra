# Copy to a local .env or pass via --dart-define. Never commit real secrets.
# The anon key is public by design; the service role must never enter this file.

SUPABASE_URL=
SUPABASE_ANON_KEY=
LUMEN_CONTROLLER_NAME=Lumen controller (placeholder)
LUMEN_CONTROLLER_EMAIL=privacy@example.invalid
LUMEN_ORIGIN=http://localhost
