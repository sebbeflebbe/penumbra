# Chapter 01 — Stack

Flutter 3.44 web. Forui 0.22.2 (SDK-matched). Riverpod. go_router. `cryptography` for AES-GCM and Argon2id.

Default backend is an in-memory studio that implements the same contracts as the planned Supabase EU path, so CI and reviewers do not need secrets. Cloudflare Pages is the free static host.

Google Fonts CDN was rejected: Forui already ships Inter; a font CDN is a processor, a transfer, and a CSP exception.
