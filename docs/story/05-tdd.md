# Chapter 05 — TDD and CI

Domain tests first: crypto round-trip, recovery checksum, RLS isolation, export/erasure, restriction.

CI: `dart analyze`, `flutter test`, OSV on `pubspec.lock`, CycloneDX SBOM artifact, optional Cloudflare Pages deploy.
