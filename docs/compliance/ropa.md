# Record of processing (GDPR Art. 30)

Controller: placeholder via `PENUMBRA_CONTROLLER_NAME` / `PENUMBRA_CONTROLLER_EMAIL`.
Purpose: operate a private visual thinking studio.
Categories: account identifiers, board metadata, encrypted nodes, consent events, security events.
Recipients: none commercially; processors listed in `processors.md`.
Retention: account life; security logs 90 days.
TOMs: TLS, AES-256-GCM at rest for nodes, RLS, re-auth for erasure.
Transfers: see `tia-us-processors.md`.
